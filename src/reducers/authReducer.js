
const initialState = {
    checking: true,
    // uid:null,
    // name
}

export const authReducer = (state = initialState, action) => {
    switch (action.type) {

        default:
            return state
    }
}